

alert('Hello, world!')

console.log('Hello, world!')

// Isso é um comentário!

let nome = 'Gabriel'

const constante = 14

var idade = 19 // Declarar variáveis com var não é mais utilizado. Atualmente utiliza-se let.

let mensagem = nome + ' tem ' + idade + ' anos.'

alert(mensagem)

// Nova forma de concatenar variáveis
alert(`${nome} tem ${idade} anos.`)

alert(mensagem.length)

// Primeira letra da mensagem
alert(mensagem.charAt(0))

// Última letra da mensagem
alert(mensagem.charAt(mensagem.length - 1))

alert(mensagem.substring(0,7))

// Mensagem em letra maiúscula
alert(mensagem.toUpperCase())

// Mensagem em letra minúscula
alert(mensagem.toLowerCase())

// Corta a partir da posição 2
alert(mensagem.slice(2))

// Retira os espaços no final
alert(mensage.trim())

